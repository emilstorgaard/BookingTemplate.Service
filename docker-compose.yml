version: '3.8'

services:
  booking-template-service-api:
    container_name: booking-template-service-api
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - '5001:8080'
    environment:
      - ASPNETCORE_ENVIRONMENT=Production